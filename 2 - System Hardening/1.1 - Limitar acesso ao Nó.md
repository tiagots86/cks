## Limitando acesso ao node kubernetes

```shel

id root

id david

/etc/passwd

passwd david

userdel david

groupdel devs

usermod -s /usr/sbin/nologin himanshi

useradd -d /opt/sam -s /bin/bash -G admin -u 2328 sam
```