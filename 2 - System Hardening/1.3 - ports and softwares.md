## Bloqueio de portas e softares

```shel
apt list --installed

systemctl list-units --type service

ls mod

vim /etc/modprobe.d/blacklist.conf

apt remove nginx

netstat -natp | grep 9090

apt install wget

```